{
	"name": "thevgergroup/notion-sync",
	"description": "Bi-directional synchronization between Notion and WordPress",
	"type": "wordpress-plugin",
	"license": "GPL-3.0-or-later",
	"require": {
		"php": ">=8.0",
		"woocommerce/action-scheduler": "^3.9"
	},
	"autoload": {
		"psr-4": {
			"NotionWP\\": "src/",
			"NotionSync\\": "src/"
		}
	},
	"config": {
		"optimize-autoloader": true,
		"sort-packages": true
	},
	"scripts": {
		"test": [
			"@test:unit"
		],
		"test:unit": "phpunit --configuration ../phpunit.xml",
		"lint": [
			"@lint:phpcs",
			"@lint:phpstan"
		],
		"lint:phpcs": "phpcs --report=summary",
		"lint:phpcs:full": "phpcs",
		"lint:phpstan": "phpstan analyse --memory-limit=1G --no-progress --error-format=table",
		"lint:fix": "phpcbf",
		"lint:fix:quiet": "phpcbf --quiet"
	}
}
